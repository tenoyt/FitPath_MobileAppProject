<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- ✅ Material 3 Light theme with your colors -->
    <style name="Theme.FitPath.NoActionBar" parent="Theme.Material3.Light.NoActionBar">
        <!-- Primary brand color -->
        <item name="colorPrimary">#03DAC5</item>
        <item name="colorOnPrimary">#000000</item>
        <item name="colorPrimaryContainer">#018786</item>

        <!-- Secondary color -->
        <item name="colorSecondary">#03DAC5</item>
        <item name="colorOnSecondary">#000000</item>

        <!-- Background and surface colors -->
        <item name="colorSurface">@android:color/white</item>
        <item name="colorSurfaceVariant">@android:color/white</item>
        <item name="colorOnSurface">@android:color/black</item>

        <!-- Status bar color -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryContainer</item>
    </style>

</resources>
